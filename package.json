{
  "name": "pektas",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --base=pektas",
    "preview": "vite preview",
    "preDeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^6.2.0"
  },
  "dependencies": {
    "izitoast": "^1.4.0",
    "simplelightbox": "^2.14.3"
  }
}
